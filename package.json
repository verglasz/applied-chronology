{
  "name": "interview-tracker",
  "version": "0.1.0",
  "description": "A web app to keep track of job applications",
  "main": "dist/server.js",
  "scripts": {
    "prebuild": "npx eslint src",
    "build": "npx tsc",
    "prestart": "npm run build",
    "start": "node .",
    "dev": "npx ts-node-dev --respawn --pretty --transpile-only src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Valerio Lomanto (lomanto@kth.se)",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.1",
    "pg": "^8.7.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.21.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "eslint": "^8.18.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4"
  }
}
